{
  "name": "coming-soon-web",
  "version": "1.0.0",
  "description": "countdown-site",
  "main": "index.js",
  "scripts": {
    "dev":"dead-server --port=4002 --host=localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Ringaudas~",
  "license": "MIT",
  "devDependencies": {
    "dead-server": "^1.0.6"
  }
}
